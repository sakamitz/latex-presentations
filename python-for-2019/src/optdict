def w(x, **y):
    pass

# 正确的调用
w(1)            # x=1, y={}
w(1, a=2, b=3)  # x=1, y={'a':2, 'b':3}

# 同样，要传入一个字典，也可以先将其解构
# 但注意，字典的键只能是字符串
d = {'a':2, 'b':3}
w(1, **d)  # y = {'a':2, 'b':3}

# 语法正确，但结果不一定是你所想的那样
w(1, y=d)  # y = {'y': {'a': 2, 'b': 3}}