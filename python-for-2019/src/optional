def h(x, *y):
    pass

# 正确的调用
h(1)      # x=1, y=()
h(1,2)    # x=1, y=(2,)
h(1,2,3)  # x=1, y=(2,3)

# 注意：不能直接用关键字形式给y传值
t = (2,3)
h(1, y=t) # 错误
# 要实现上述功能，可以先将元组或列表解构
h(1, *t)      # 正确，y=(2,3)
h(1, *[2,3])  # 正确，y=(2,3)